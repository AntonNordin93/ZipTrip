@page
@model ZipTrip.Pages.Trip.CreateModel
@{
    ViewData["Title"] = "Plan Your Trip";
    ViewData["HideHeader"] = true;
    ViewData["HideFooter"] = true;
}
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

<div class="flex flex-col lg:flex-row flex-1 bg-background pt-8 lg:pt-0 h-screen overflow-hidden text-foreground">
    <div id="sidebar-container" class="w-full lg:w-1/3 xl:w-1/4 p-6 overflow-y-auto border-r border-border bg-card z-10 shadow-xl relative">
        <partial name="_CreateFormPartial" model="Model" />
    </div>

    <div class="flex-grow relative h-[50vh] lg:h-auto z-0">
        <div id="trip-map" class="absolute inset-0 w-full h-full"></div>

        <div id="loading-overlay" style="display: none; z-index: 99999;" class="fixed inset-0 bg-background/90 backdrop-blur-md items-center justify-center flex-col">
            <div id="loader-box" class="bg-card border border-border px-10 py-8 rounded-[32px] flex flex-col items-center gap-6 text-center max-w-xs w-full transition-shadow duration-300">
                <div class="relative w-16 h-16">
                    <div id="loader-ring-bg" class="absolute inset-0 border-4 rounded-full opacity-20"></div>
                    <div id="loader-ring-spin" class="absolute inset-0 border-4 border-t-transparent rounded-full animate-spin"></div>
                </div>
                <div class="w-full">
                    <h3 class="text-2xl font-display font-bold text-foreground mb-1" id="loading-title">Scanning...</h3>
                    <div class="flex justify-between items-end mb-2 mt-4">
                        <span id="loading-status-text" class="text-[10px] uppercase tracking-widest text-muted-foreground font-bold">Connecting...</span>
                        <span id="loading-percentage" class="text-lg font-bold">0%</span>
                    </div>
                    <div class="w-full h-2 bg-muted rounded-full overflow-hidden">
                        <div id="loader-progress" class="h-full w-0 transition-all duration-300 ease-out"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .fade-in { animation: fadeIn 0.4s ease-in-out; }
    @@keyframes fadeIn { from { opacity: 0; transform: translateX(-10px); } to { opacity: 1; transform: translateX(0); } }
</style>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="~/js/trip-create.js" asp-append-version="true"></script>
}